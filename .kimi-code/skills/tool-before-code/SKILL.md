---
name: tool-before-code
description: Use before writing custom scripts or manual implementations.
---

Check whether the repo already has a command, package script, just recipe, MCP server, CLI, linter, formatter, or test runner that does the job. Prefer the existing tool.
