# Goal Prompt

Read `AGENTS.md` (canonical rules) and `KIMI.md` (run/analysis commands). The detailed layer/mouse/workflow policy text formerly duplicated here was removed — `AGENTS.md` is the single source of truth for those rules.

One operational note kept from the old file: never hand-edit the generated `evolved_apply.js` / `evolved_verify.js` export artifacts; fix the exporter and regenerate instead.
