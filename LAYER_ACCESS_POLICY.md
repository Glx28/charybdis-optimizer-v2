# Layer Access Policy

This document was merged into `AGENTS.md`, which is now the single source of truth for layer-access, dynamic-layer, mouse-layer, familiarity, and thumb-clearance rules. The old copy here had drifted out of date (it predated the 2026-07-13 MB same-row / MB4-MB5 exact-adjacency requirements, the self-referential-access exclusion, and the 30,000-generation run target).
